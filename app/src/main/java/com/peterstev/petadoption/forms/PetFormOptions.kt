package com.peterstev.petadoption.forms

class PetFormOptions {
    enum class TextInputType {
        EMAIL, PASSWORD, NUMBER, NAME, DATE
    }

    enum class CheckedAction {
        SHOW, HIDE
    }
}
